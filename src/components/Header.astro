---
import { Image } from 'astro:assets';
import logoImg from '../assets/golf-logo.png';

// TODO: Update navigation items and links as needed
const navItems = [
  { label: 'Course', href: '#' },
  { label: 'Practice', href: '#' },
  { label: 'Lessons', href: '#' },
  { label: 'About', href: '#' }
];
---

<header class="bg-white shadow-md">
  <div class="container mx-auto px-4 py-4 flex justify-between items-center">
    <a href="/" class="flex font-serif font-bold items-center">
      <Image 
        src={logoImg} 
        alt="Company Logo Image" 
        class="rounded-lg"
        width={48}
        height={48}
      />
      <span class="ml-2">Milt's Golf Center</span>
    </a>
    <nav class="hidden md:flex space-x-6">
      {navItems.map(item => (
        <a href={item.href} class="text-gray-700 hover:text-primary-500 transition duration-150 ease-in-out">
          {item.label}
        </a>
      ))}
    </nav>
    <a href="/#" class="bg-primary-500 text-white px-4 py-2 rounded hover:bg-primary-600 transition duration-150 ease-in-out">
      Contact Us
    </a>
  </div>
</header>
